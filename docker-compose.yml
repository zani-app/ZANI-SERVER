version: '3.8'

services:
  api:
    image: zani-api
    container_name: zani-api-container
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
    restart: always
